export const localReducer = (
    state = {
        media: [],
        isFetching: false,
        error: undefined,
    }, action) => {
    
    switch(action.type) {   
        default:
            break;
    }
    return state;
};